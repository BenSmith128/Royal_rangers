# This works for initial creation only.
# When changes are observed, there is longer code.

CREATE TABLE `Royal_rangers.fromyear_toyear_unchartered_list` AS (
  SELECT
    ROW_NUMBER() OVER (ORDER BY First_att, Unch_name) AS Unch_num,
    Unch_name,
    First_att
  FROM `Royal_rangers.year_unch_o1`
)
